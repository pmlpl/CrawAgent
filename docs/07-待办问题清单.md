# 07 · 待办问题清单

> 2026-08 全量代码扫描重写。P0 是当前最明显的短板，P1 是代码质量，P2 是功能增强。接手开发建议从 P0 开始。

## P0 — 应尽快解决

### 1. ~~生产代码残留调试打印~~（已修复 2026-08-29）

- 位置：`crawagent/web/server.py` 原 L221-238、L277-284
- 处理：已删除全部 `[CACHE_DEBUG]` print 语句，`crawagent/graph/metrics.py` 统计逻辑未动

### 2. ~~reveal-key 端点无鉴权~~（已修复 2026-08-29）

- 位置：`crawagent/web/server.py` 原 L668-677
- 处理：直接删除该端点（明文 Key 不再对外提供）；前端"眼睛"按钮一并移除，API Key 恒为 password 脱敏展示，`/api/settings` 的 `sk-****` 脱敏展示足够 UI 使用

### 3. ~~ScriptForcer 失败阈值文档与代码不一致~~（已修复 2026-08-29）

- 位置：`crawagent/graph/script_forcer.py` L13
- 处理：docstring 已统一为代码实际值（< 50 字符），阈值未改动

### 4. 测试覆盖仍不足（已有改善）

- 现状：`tests/test_smoke.py`（5 个：可导入/Agent 构建/社媒解析/置信度/usage 映射）+ `tests/test_pure_tools.py`（confidence 分支、裁剪中间件、列表正则）
- 缺失：
  - `ScriptForcerMiddleware` 全触发链测试（3 次失败/5 次硬限/30 次总量/连续同工具）
  - `web/server.py` 接口测试（FastAPI TestClient：settings 脱敏回写、批量删除跳过运行中会话、事件日志重放）
  - 前端 useChat.js 事件分发（Vitest）
- 顺带修：~~`tests/test_smoke.py` L19 注释仍写"12 个工具"~~（已改为 17）

## P1 — 代码质量（近期做）

### 1. 前端死代码清理

- `web/src/components/NavBar.vue`（160 行）、`SessionSidebar.vue`（158 行）：全项目零 import 引用，是旧版布局遗留
- `screens/` 目录 3 张调试截图（typing_during/after/reload.png）为开发遗留
- 建议：直接删除，git 历史里都有

### 2. web/server.py（890 行）待拆

- 批量删除 + 事件日志加入后持续膨胀；WebSocket 处理、sessions/settings/sites 三组 API、事件泵全在一个文件
- 建议：按 FastAPI APIRouter 拆 `routers/sessions.py`、`routers/settings.py`、`routers/sites.py`；`_run_turn`/`_stream_turn` 事件泵抽独立模块
- 附带清理：L9/L257/L462 注释引用已删除的 `main.py` CLI；L349 `except (WebSocketDisconnect, Exception)` 冗余（Exception 已覆盖）

### 3. social_tool.py（556 行）待拆

- 抖音 + B 站两平台逻辑挤一个文件（与上版清单一致，仍未做）
- 建议：拆 `douyin_tool.py` + `bilibili_tool.py`，共享下载函数留 utils

### 4. ScriptForcer 重置的私有属性耦合

- 位置：`crawagent/web/server.py` L118-119 访问 `agent._script_forcer._reset()`
- 风险：跨模块访问两层私有属性；未来加 CLI/其他入口会漏掉重置，导致跨轮失败累积误触发
- 建议：在 agent.py 暴露公开方法 `agent.reset_turn_state()`，或把重置逻辑挪进 middleware 的 `wrap_model_call` 首次调用

### 5. 资源累积无上限

- `_metrics` / `_session_locks` 字典只增不减（仅删除会话时清理）
- `logs/` 会话归档 JSON 永久累积
- `output/` / `downloads/` 产物无保留策略、无容量上限
- 建议：统一加按日期/容量的清理工具，settings 加 `output_max_age_days`；内存字典加简单的 LRU 上限

### 6. 注释/文档级小坑

- `middleware.py` L48-57：注释说"保留首尾 500 字符"，实际实现是头 250 + 尾 125（`max//2` + `max//4`）
- `server.py` L546-547：`fut.cancel()` 对运行中的 executor future 无效（线程不可取消），真正生效的是 `cancelled` 标志位——代码有误导性
- 均为一行级修正，顺手改

### 7. 无 CI/CD、无类型检查

- 无 GitHub Actions / pre-commit；未配 mypy / pyright
- 建议：最小 CI（`uv sync` + `pytest tests/`）即可防低级回归

## P2 — 功能 TODO（择机做）

| # | 功能 | 说明 | 难度 |
|---|------|------|------|
| 1 | 会话重命名 | 现在只有 hex session_id，侧栏加可读名字（建 session 元数据表或复用 sites.json 模式） | 低 |
| 2 | 会话导出 | 导出为 markdown/PDF（归档 JSON 已有数据源，加一个转换下载端点） | 低 |
| 3 | 深度爬取/翻页通用化 | 自动翻页目前仅壁纸工具内置；通用列表页批量抓取靠 run_custom_script（BATCH-FIRST 规则已缓解） | 高 |
| 4 | Pinia 状态管理 | useChat.js 317 行持续增长，规模扩大后迁 Pinia | 中 |
| 5 | 限流/代理池 | 无内置 rate limiter、无代理轮换（`request_delay` 是唯一节流手段） | 中 |
| 6 | WebUI 鉴权 | 无登录（与 P0-2 关联，部署公网前必做） | 中 |
| 7 | MySQL/PostgreSQL 迁移 | 现在只有 SQLite | 中 |
| 8 | LangSmith 面板集成 | Key 已支持，前端无追踪入口 | 低 |
| 9 | ~~生态扩展：内置爬虫 skills 包 + 多 MCP 接入 + 插件规范~~ | ✅ 已完成（2026-09-05，详见下节） | 高 |

### P2-9 生态扩展（✅ 2026-09-05 完成）

交付内容（三项全部落地，验收标准达成）：

1. **内置爬虫 skills 包** ✅：`skills/` 内置 6 个技能——B站/抖音/壁纸站/微信读
   书站点攻略 + `batch-crawl-playbook`（BATCH-FIRST 落地手册：一次脚本循环八要点）
   + `custom-script-recipes`（S1-S4 各级骨架 + helper 速查 + 诊断对照表）。索引段
   约 975 字符注入 system prompt，正文 `read_skill` 按需读。
2. **第三方爬虫 MCP** ✅：接入 fetch（官方 mcp-server-fetch，stdio，1 工具）与
   playwright（微软 @playwright/mcp，stdio，24 工具），与 anything-analyzer 并列
   共 3 个 server。`get_mcp_tools()` 重构为**逐 server 连接+缓存+故障隔离**（一个
   挂了不拖累其它，失败不缓存自动重试）；新增 `{project_root}` 占位符。Windows
   实测结论：uvx 每次启动重新下载依赖导致握手超时（弃用，改 venv 直拉）；
   `npx @latest` 逐次查 registry（弃用，改全局安装 .cmd 直连）；mcp 1.29.1 在
   Proactor 循环下正常，无需 Selector policy。`.env.example` 已写推荐配置。
3. **插件规范** ✅：`plugins/<name>/` + `plugin.json` manifest + `tools/*.py` +
   `skills/*/SKILL.md` 约定；`discover_tools()` 扫描插件（内置重名优先）；CLI 三
   件套 `add-tool`（脚手架，重定向到 plugins/ 并修复了旧版产出永不被发现的 bug）/
   `add-plugin`（本地路径或 git URL 一键接入）/ `plugins`（列表）；示例插件
   `plugins/example-rss/` 随仓库交付。

验收记录：121 pytest 全绿（+4 新测试）；Agent 构建含 21 内置/插件工具 + 25 MCP
工具实测通过；README、docs/04、`.env.example` 已同步。firecrawl-mcp 调研结论：
强制 FIRECRAWL_API_KEY 云端付费，与本地部署原则冲突，不默认接入（文档已注明装法）。

**后补 UI（2026-09-06）**：设置页新增「生态面板」——MCP 服务表（状态/工具数/启用
开关/增删/添加表单，token 脱敏回写保护）、技能列表（名称/描述/来源）、插件列表
（含「打开目录」按钮与接入指引）。新增 API：`GET /api/ecosystem`、
`POST /api/mcp/servers/save`（保存即清三层缓存+预热握手，免重启生效）、
`POST /api/ecosystem/open-folder`。`get_mcp_tools` 支持 `disabled` 标记。测试 124 全绿。

## 已知历史 Bug（已修复，留档防回归）

- **React #185 最大更新深度**（删除会话）：后端未清理 `_active_turns` 中运行中任务 + 前端删除竞态 → 删除时先取消任务再清检查点，前端乐观更新 + 延迟重连
- **刷新后 UI 永远"思考中"**：单消费者队列在 WS 死亡时吞掉 `done` 事件 → 改为事件日志 + cursor 增量重放（`_emit`/`_stream_turn`），重连从 0 全量补发
- **重启后丢失对话上下文**：早期 chat 模式绕过 LangGraph 手拼历史 → 已删除双模式，统一走 create_agent + checkpointer（thread_id 自动加载/持久化）
- **设置面板 Key 覆盖 Bug**：脱敏掩码被回写覆盖真实 Key → 前后端统一"含 `*` 即掩码"判断
- **缓存命中率假数据**：langchain-openai 1.5.0 只映射 cache_read 不映射 miss → `metrics.py` 从 `response_metadata.token_usage` 兜底取 miss
- **openai 版本锁定**：pyproject 显式 `openai>=2.26.0,<3.0.0`——被篡改版本用 httpx2 劫持 httpx 命名空间会触发 SyncByteStream 断言失败
- **背景图"部分图设不了"**（2026-09-06 修）：根因是 MB 级壁纸 base64 后顶爆 localStorage ~5MB 配额（按 UTF-16 计费≈原始字节 2.7 倍），且 setBg 的 catch 静默吞错被误读为格式不支持 → 上传统一 canvas 压缩（1920px/JPEG q0.82）+ 所有失败路径显式报错（`9ec6f88`）。再动这段保持"失败必须报错"，别静默 catch
- **「已耗时 Xs」冻结**（2026-09-06 修）：pill 里的耗时是后端心跳静态快照，工具结束/思考阶段残留不更新 → 前端 startedAt+每秒 tick 实时计算（useChat.runningElapsed），后端心跳文本只留轨迹日志（`8e0b82b`）

## 验证基线

改动后至少跑通：

```bash
# 1. 测试
uv run pytest tests/ -v

# 2. Agent 构建
python -c "from crawagent.graph.agent import get_agent; get_agent()"

# 3. WebUI 冒烟（服务起得来 + WS 事件序列完整）
uv run python -m crawagent.web.server
# 另开终端：ws 连接 → 发消息 → 收到 tool_call/.../ai_delta/done 序列
# 刷新页面 → 历史恢复 + 若任务在跑收到 resumed
```
