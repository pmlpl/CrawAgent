# ====================================
# CrawAgent 依赖
# ====================================
# 安装命令:
#   pip install -r requirements.txt
# ====================================

# ---- 核心框架 ----
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.3.0

# ---- HTTP / 解析 ----
httpx>=0.27.0
beautifulsoup4>=4.12.0
requests>=2.28.0

# ---- 网络抓包爬虫 (DrissionPage 监听浏览器网络请求) ----
DrissionPage>=4.0.0
# 注：DrissionPage 需要系统已安装 Chrome/Edge 浏览器，首次运行会自动下载驱动

# ---- Scrapling 高级爬取 (TLS指纹/隐身浏览器/自适应解析) ----
scrapling>=0.4.0
curl_cffi>=0.7.0
browserforge>=0.14.0
patchright>=1.0.0
msgspec>=0.18.0

# ---- 终端美化（可选，缺了也能跑，只是丑一点） ----
rich>=13.7.0
prompt_toolkit>=3.0.0

# ---- 环境变量加载 ----
python-dotenv>=1.0.0

# ---- FastAPI Web 接口 (Phase X) ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# ---- 浏览器自动化 (Phase 2) ----
playwright>=1.40.0
# 安装后需运行: playwright install chromium

# ====================================
# 可选依赖 —— 按需安装
# ====================================
#
# 👉 用本地 transformers 加载 GGUF/权重？安装这些：
# transformers>=4.40.0
# torch>=2.2.0
#
# 👉 用 LangGraph 可视化（后续 Phase 扩展）：
# langgraph>=0.2.0
