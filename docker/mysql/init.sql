-- CrawAgent MySQL 初始化脚本
-- 字符集设置
ALTER DATABASE crawagent CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 授权（如果需要远程访问）
-- GRANT ALL PRIVILEGES ON crawagent.* TO 'crawagent'@'%';
-- FLUSH PRIVILEGES;
